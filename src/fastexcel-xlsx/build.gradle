dependencies {
    compile(
           project(":fastexcel-stream")
    )
}

coverage {
    includeClasses = [
    ]
    lineMini = 0.0
    classMini = 0.0
}

style {
    excludePackages = [
    ]
    excludeClasses = [
    ]
}